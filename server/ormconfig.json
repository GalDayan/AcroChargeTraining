{
  "type": "postgres",
  "host": "localhost",
  "username": "admin",
  "password": "admin",
  "synchronize": true,
  "logging": true,
  "database": "demo",
  "port": 5432,
  "migrations": ["./src/migration/**/*.ts"],
  "entities": ["./src/**/*.entity.ts"],
  "cli": {
    "migrationsDir": "src/migration"
  }
}
